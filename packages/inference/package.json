{
  "name": "@osai/inference",
  "version": "0.1.0",
  "private": true,
  "description": "Inference adapter contract, connecting the job queue to Project.recordGeneration",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@google/genai": "^2.20.0",
    "@osai/core": "*",
    "@osai/jobs": "*",
    "google-auth-library": "^10.9.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22"
  }
}
